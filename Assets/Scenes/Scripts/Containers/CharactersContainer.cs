using System.Collections.Generic;
using UnityEngine;

/*public class CharactersContainer : MonoBehaviour
{
    public static CharactersContainer Instance;
    
    public List<BaseCharacter> characters = new List<BaseCharacter>();

    private void Awake() => Instance = this;
    private void OnEnable() => ActionContainer.Instance.OnInitializedAllCharacters += InitializeNewCharacter;
    private void OnDisable() => ActionContainer.Instance.OnInitializedAllCharacters -= InitializeNewCharacter;
    private void InitializeNewCharacter(BaseCharacter character) => characters.Add(character);
}*/
